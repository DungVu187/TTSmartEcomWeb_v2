# Ma trận truy cập endpoint

Bản chụp: 2026-08-20. Tài liệu này ghi lại các attribute/kiểm tra authorization đã triển khai, không phải chứng nhận bảo mật. Mọi route đã khai báo cũng chấp nhận alias `/api`.

Phase 3A: Control Plane identity được tải lại từ `[ttsmart.com.vn]` trước authorization. Với user Control Plane, permission policy dùng active Company/Branch scope; không dùng `AdminFullAccess` để cấp quyền global. `X-Company-Id`/`X-Branch-Id` chỉ chọn membership đã xác thực và cặp company/branch lệch nhau bị từ chối. Các endpoint mới có target object phải gắn `ScopeAuthorize` và kiểm tra target sau model binding; bảng dưới không được coi là bằng chứng đã bao phủ mọi route object-scope.

Company schema v1 là DDL test-only, không thêm HTTP endpoint và không đổi policy runtime trong lát cắt này. Khi thêm mutation Product Master dùng Company DB, endpoint mới phải yêu cầu scope Company đáng tin cậy, permission Company (ví dụ `product.master.create`), feature phù hợp, kiểm tra ownership resource và ghi audit Company; không tin CompanyId/BranchId từ client.

Admin frontend chỉ gửi scope từ lựa chọn membership đã trả về bởi `/users/profile`; giá trị lưu browser bị xóa khi login/logout. Backend vẫn là boundary quyết định: header do client gửi không thay thế authorization object-scope.

Quyết định kiến trúc ngày 2026-08-24: `Company` và `Branch` là scope/entity, không phải role. Quyền cho phép phải thỏa authentication, membership, scope, permission, feature và resource ownership. Platform SuperAdmin được bypass permission thông thường ở cả Control Plane và Operational, nhưng không bypass kiểm tra database assignment, input safety hoặc Activity History. Mutation nghiệp vụ luôn phải resolve đúng một Branch; scope “Tất cả chi nhánh” chỉ dành cho đọc/tổng hợp dashboard.

| Nhóm route | Số lượng legacy | V2 đã khai báo | Public / tùy chọn | Người dùng đã authentication | Role / permission | Ghi chú cấp đối tượng và triển khai | Bằng chứng test |
|---|---:|---:|---|---|---|---|---|
| `/users` | 30 | 30 | login, admin login, logout, register, recovery và autologin theo policy | profile, address, template, change-password, my-stations | permission admin/customer/account; danh mục permission dành cho admin/superadmin | Identity hiện tại được tải lại từ Mongo. `UserAdministrationPolicy` kiểm tra role đích trước mutation; repository ghi/xóa kèm `expectedRole`; tạo/nâng vai trò Super Admin đi qua mutex phân tán Mongo. Recovery không tiết lộ OTP và trả lỗi xác định khi email provider chưa cấu hình. | Policy/role đích/guard 7/7 và guard Mongo 1/1; Security gồm admin ngang cấp/guard; endpoint Mongo user dương tính còn mở |
| `/products` | 35 | 35 | catalog/type/detail/list/top-purchased/review và các batch public dùng viewer ẩn danh hoặc tùy chọn | mutation review cần identity đang hoạt động; batch inventory cần admin đã authentication hoặc nhân viên có permission inventory | `product.*`, `order.scan_ai`; điều chỉnh stock và chi tiết admin cần `product.edit` | Media image/document, xóa variant image và dọn temp invoice image đã có validation/containment/reference check. AI/voice có ranh giới provider. Customer có station được gán chỉ thấy hợp sản phẩm của các station đó; `stationId` sai định dạng trả 400 và station ngoài phạm vi được gán trả 403. Public detail/batch luôn loại `importPrice`/`earn`; admin-detail và inventory batch phải chủ động chọn projection riêng tư. | Contract dương tính Product AI/media; Unit scope/media/BSON/projection riêng tư; endpoint Mongo dương tính diện rộng còn mở |
| `/chips`, `/chips/types` | 21 | 21 | mọi lượt đọc và batch ảnh section | — | thao tác ghi và media dùng `product.create`; xóa media kiểm tra reference | Không có quyền sở hữu đối tượng | Unit media; chưa có positive endpoint |
| `/carts` | 6 | 6 | — | cả sáu | đã authentication | Chỉ dành cho người dùng hiện tại; service kiểm tra khả năng hiển thị sản phẩm/station và trạng thái chỉ liên hệ | `getCart` khi chưa authentication, gồm cả alias `/api` |
| `/manages` | 24 | 24 | GET manage/policies | — | mutation dùng `storefront.manage` | Bốn route multipart và delete-image đã có validation/path containment; cần review consistency khi xóa file vật lý sau cập nhật Mongo | Boundary auth cho route file; chưa có positive endpoint |
| `/orders` | 20 | 20 | số lượng đang xử lý | khách hàng create/list/detail/cancel/delete | `order.view/create/edit` cho quản trị | Read/cancel/delete của khách hàng kiểm tra số điện thoại; admin/staff được bỏ qua kiểm tra này. Upload/delete invoice image đã triển khai; bốn event Socket.IO và notification Gmail/Telegram/Zalo được phát best-effort sau commit cho order khách hàng | Unit side effect/notification, Contract ranh giới và Integration Socket.IO; endpoint Mongo order dương tính diện rộng còn mở |
| `/iporders` | 17 | 17 | — | — | `iporder.view/create/edit/delete`; giữ khả năng tương thích admin-full-access | Completion bulk/line, aggregate `/products`, upload/delete image và điều chỉnh stock khi thêm/cập nhật dòng đã có. Ghi stock dùng CAS/compensation; payload tạo đơn đã có tiến độ/trạng thái bị validation 400. Không có route hoãn hoặc lỗi 501. | Unit stock/compensation/aggregation, boundary auth/media và integration aggregation Mongo chọn lọc; chưa có positive Mongo endpoint rộng |
| `/eporders` | 17 | 17 | — | — | `eporder.view/create/edit/delete`; giữ khả năng tương thích admin-full-access | Completion bulk/line, aggregate `/products`, upload/delete image và điều chỉnh stock khi thêm/cập nhật dòng đã có. Ghi stock dùng CAS/compensation; payload tạo đơn đã có tiến độ/trạng thái bị validation 400. Không có route hoãn hoặc lỗi 501. | Unit stock/compensation/aggregation, boundary auth/media và integration aggregation Mongo chọn lọc; chưa có positive Mongo endpoint rộng |
| `/stations` | 12 | 12 | search/public/code/by-codes/by-ids | — | `station.view/create/edit/delete` | Route public dùng projection tường minh theo hình dạng legacy; upload/remove image có validation/containment. Root static và header nội dung vẫn cần xác minh khi triển khai | Ranh giới auth/media; chưa có endpoint Mongo dương tính rộng |
| `/histories` | 4 | 4 | — | — | permission lịch sử import/export được chọn theo hướng; một trong hai permission cho detail/filter/update | Giữ khả năng tương thích `AdminFullAccess` | Chỉ có danh sách khi chưa authentication |
| `/activity-logs` | 1 | 1 | — | — | `activitylog.view` | Route đọc có mapping nhãn/reference legacy; 48 mutation route ghi 44 action legacy theo best-effort sau commit, với actor từ identity đang sống và payload đã redact | Unit hình dạng/redaction/BSON, Contract side effect/lỗi, Integration query Mongo; Contract/Security xác nhận chưa đăng nhập bị từ chối |
| `/zalo` | 4 | 4 | callback public theo protocol; settings/auth-url dành cho admin hoặc superadmin | — | role admin; auth-url phát state có chữ ký, dùng một lần và gắn với subject | OAuth auth-url/callback đã trao đổi provider qua adapter có timeout/cancellation/giới hạn response; state có chữ ký, hết hạn và chỉ dùng một lần; token/payload provider không được trả hoặc log | Unit state/service/client, Contract OAuth dương tính với adapter giả, Security state giả mạo/thiếu |
| `/telegram` | 6 | 6 | — | — | admin hoặc superadmin | Test-send gọi adapter Telegram nếu bot token có; thiếu token trả lỗi cấu hình. Order khách hàng chỉ gửi tới recipient được bật có event `new_order`; HTTP client có tên tắt URI logger vì token nằm trong path | Unit provider/notification, Contract dương tính với adapter giả, Security ranh giới; chưa gọi provider production |
| `/voice-vocabs` | 4 | 4 | — | — | `voice.manage` | Persistence Mongo, cache runtime và khởi tạo seed/backfill có fail-safe; mutation refresh cache và ghi ActivityLog best-effort | Unit service/runtime/audit; GET 401; chưa có Mongo endpoint integration |

## Biện pháp kiểm soát xuyên suốt và khoảng trống

- JWT chỉ được đọc từ cookie `authToken`. Với tài khoản nội bộ, middleware tải lại identity Control Plane và membership đang hoạt động trước authorization; khách hàng legacy tiếp tục dùng identity adapter Operational/Mongo.
- Chính sách permission cấp quyền truy cập cho Platform SuperAdmin theo bypass đã chốt. `AdminFullAccess` có thể cấu hình cho role admin legacy vẫn chỉ là quyết định tương thích và không được suy rộng thành quyền Platform SuperAdmin.
- `LegacyCsrfOriginMiddleware` từ chối yêu cầu không an toàn có cookie `authToken`, trừ khi yêu cầu có đúng một `Origin` trong allowlist, một `Referer` có origin trong allowlist hoặc, khi thiếu cả hai, Fetch Metadata bằng `same-origin`. Kiểm thử xác nhận origin cùng site nhưng khác origin với giá trị `same-site` bị từ chối. Chính sách vẫn đang mở vì chưa được xác minh với trình duyệt/staging phía sau reverse proxy.
- CORS allowlist chỉ cho phép thông tin xác thực đối với các origin được cấu hình.
- Ba nhóm quản trị dùng kiểm tra role trực tiếp (`telegram`, `zalo`, danh mục permission), trong khi phần lớn nhóm khác dùng chính sách permission động.
- Đã có kiểm thử âm tính cho sai role/đối tượng đích trong quản trị user và ranh giới authorization ở nhiều nhóm; độ bao phủ dương tính/âm tính giữa các đối tượng và endpoint Mongo cho toàn bộ ma trận vẫn chưa đầy đủ.
- Cổng backend Phase 3A đạt 362/362 test ở lần chạy có SQL Server test cô lập: Unit 245, Contract 53, Integration 27 và Security 37. Đây là bằng chứng hồi quy tại checkpoint, không phải một test authorization dương tính/âm tính cho từng endpoint.
